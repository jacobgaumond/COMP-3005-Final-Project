services:
  final-project-postgres-container:
    image: postgres:14.19
    volumes:
      - type: bind
        source: ./container-volume/setup.sql
        target: /docker-entrypoint-initdb.d/setup.sql
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: assignment-password
